# BOOTSTRAP.md - RT-Recon

## First Run
1. อ่าน `SOUL.md` (รวม Identity, Kali Tools, Rules ทั้งหมด)
2. ยืนยัน scope จาก `rt-lead` ผ่าน `session_state.json`
3. อ่าน `docs/HANDOFF_PROTOCOL.md`
4. ตรวจ Kali: `nmap --version`, `which ffuf`

## ทุกครั้งที่ส่งต่อ
- เขียน handoff → `rt-webops` พร้อม `confidence` + `evidence_refs`

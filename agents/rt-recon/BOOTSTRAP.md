# BOOTSTRAP.md - RT-Recon

## First Run
1. อ่าน `SOUL.md` (รวม Identity, Jason Haddix Methodology, Kali Tools, Rules)
2. ยืนยัน scope จาก `rt-lead` ผ่าน `session_state.json`
3. อ่าน `docs/HANDOFF_PROTOCOL.md`
4. ตรวจ Kali tools: `which subfinder`, `which httpx`, `which gau`, `which ffuf`

## ทุกครั้งที่ส่งต่อ
- เขียน handoff → `rt-webops` พร้อม Top 3 Attack Paths (MITRE T-Codes) + `confidence` + `evidence_refs`

# HEARTBEAT.md - RT-Recon

Checklist:

1. มี scope ล่าสุดจาก `rt-lead` หรือยัง
2. มี finding ที่ยังไม่ validate ค้างหรือไม่
3. มี handoff ที่ยังไม่ส่งให้ `rt-webops` หรือ `rt-access` หรือไม่

ถ้าไม่มี ให้ตอบ `HEARTBEAT_OK`

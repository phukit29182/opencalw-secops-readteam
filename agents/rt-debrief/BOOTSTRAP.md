# BOOTSTRAP.md - RT-Debrief

## First Run
1. อ่าน `SOUL.md` (รวม Identity, Input Sources, Output Template)
2. อ่าน scoring rubric จาก `docs/REDTEAM_LAB_3H_BLUEPRINT_TH.md`
3. เปิด `session_state.json` — อ่าน `findings[]`, `handoffs[]`, `phases[]` ทั้งหมด
4. อ่าน `docs/HANDOFF_PROTOCOL.md` ตรวจความครบของ handoffs

## หลัง Debrief
- เขียน `score` + `metrics` ลง session state
- แจ้ง `rt-lead` ปิด session
